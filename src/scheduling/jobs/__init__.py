"""Q.66.A.4 — jobs do scheduler agrupados por domínio.

Cada submódulo expõe funções `_*_job(...)` (prefixo `_` preserva o
contrato anterior: privadas-por-convenção, accionadas pelo APScheduler).
"""
