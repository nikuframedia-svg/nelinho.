"""Sprint Q.15 — Diagnostic handlers (ERRO-TREE, Reichenbach, Mill's).

The package owns:
  * `repository` — single source of truth for diagnostic queries.
  * `types` — Hypothesis, MoldUsage, WorkerStats dataclasses shared
    across handlers.
  * (D.1+) `erro_tree`, `reichenbach`, `mill_diff` — handler modules.
"""
