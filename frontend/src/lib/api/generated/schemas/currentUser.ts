/**
 * Generated by orval v8.11.0 🍺
 * Do not edit manually.
 * ProdPlan ONE
 * Industrial ERP - Planning, Costing, HR Management
 * OpenAPI spec version: 1.0.0
 */

/**
 * Identity payload returned by /v1/auth/me.

Stable contract — frontend consumers (Sidebar chip, role-gating in
pages) depend on these field names.
 */
export interface CurrentUser {
  /** User UUID; null until JWT is wired (dev/test). */
  user_id?: string | null;
  /** Resolved tenant UUID for the request. */
  tenant_id: string;
  /** Role hint. Falls back to 'manager' until RBAC is fully wired. When JWT lands, comes from the token claim. */
  role?: string;
  /** Display name for the user chip. */
  name?: string;
  /** Display email; '—' placeholder when no User row matches. */
  email?: string;
  /** Operating mode: 'manager' (default), 'operator' (kiosk), or 'ceo' (read-only KPIs). Used by the UmweltSwitcher. */
  umwelt?: string;
}
