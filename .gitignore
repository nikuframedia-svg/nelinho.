# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv
*.db
*.sqlite
*.sqlite3
*.pyc
*.pyo
*.pyd
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/
.mutmut-cache

# Claude Code local settings (per-machine, not to be tracked)
.claude/

# Backend
backend/prodplan.db
backend/__pycache__/
backend/*.db
backend/*.sqlite

# Frontend
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/.vite/
frontend/.env.local
frontend/.env.*.local
frontend/*.local

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# Logs
*.log
logs/

# Run outputs (kept locally for diff'ing across runs, not version-controlled)
scripts/last_*.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment
.env
.env.local
.env.*.local
.env.production
.env.development

# Build artifacts
*.egg
*.egg-info
.eggs/
wheels/
*.whl

# ML registry artifacts — joblib pickles written by the training jobs;
# the registry row is in the DB, the binary is regenerable, not versioned.
ml_artifacts/

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.hypothesis/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# Celery
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# Legacy
legacy/__pycache__/

# Q.62.E.10 — Orval-generated hooks AGORA versionados (single source of
# truth para o contract OpenAPI; regerar via `npm run gen:api` quando o
# OpenAPI muda). Linha previa que ignorava `frontend/src/lib/api/generated/`
# foi removida em Q.62.E.10 apos Q.62.E.7 corrigir o erro TS no factory.ts.

# Q.62.A.1 — pg_dump backups (rotativos, grandes).
backups/

# Q.62.E.2 — Q.55 experiment console outputs (regravavel via script).
scripts/q55_run_console.txt

# Excel temp files (Office lock files)
~$*.xlsx
~$*.xls

# Cursor AI
.cursor/
.cursorignore
.cursorrules

# Additional cache
*.parquet
*.pickle
.cache/
.ruff_cache/

# Terraform (if used)
*.tfstate
*.tfstate.*
.terraform/

# Data files (optional - uncomment to exclude large data)
# *.xlsx
# *.csv

# Q.67.2.D — external data files (Folha_IA_extra.xlsx etc.) ficam fora
# do git; ver scripts/fetch_folha_ia.py para o sha256 known-good.
data/external/
!data/external/.gitkeep










models/
# Q.67.6.B5 — packages `models/` legítimos em código fonte (não dataset/ML
# artifacts). Mais granular que `!src/**/models/` para evitar bypass futuro.
!src/governance/models/
!src/core/models/
!src/copilot/models/
!src/ml/models/

# Q.61.05 — mutmut state (regravavel via scripts/mutation_test.ps1)
mutmut-cache/
.mutmut-cache/
html/  # mutmut html report output

# Q.62.E.10 — orval generated/ AGORA versionados (ver linha 135 acima).
# Esta segunda entrada foi removida; mantemos comentario para historia.

