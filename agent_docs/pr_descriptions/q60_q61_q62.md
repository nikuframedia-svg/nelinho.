# PR: feat/q60-qualidade-agentes → main (Q.60 + Q.61 + Q.62)

**Branch:** `feat/q60-qualidade-agentes`
**Target:** `main`
**Ahead:** 179 commits
**Tags incluídas:** `q61-closed`, `q62-closeout`

---

## Summary

3 campanhas consecutivas que reescreveram a arquitectura do nelinho
para "ready para prod":

- **Q.60 (~30 commits)** — Decomp dos ficheiros >1000 linhas + frontend
  test infra + design system canónico.
- **Q.61 (49 commits)** — Reestruturação detalhada: 40 sub-sprints
  fechados (28 commit + 5 no-fix + 7 decisões). Inclui audit pipeline,
  ACTION_WIRING, SoD, UoW, trace_id, KPI factory, RLS scaffolding,
  legacy migration, mutmut baseline, orval setup.
- **Q.62 (17 commits)** — Closeout que fechou as 4 promessas pendentes
  da Q.61: chain repair alembic, RLS effectivo, KPI factory consolidado
  (5 métodos inequívocos), CPO async via Arq worker + polling +
  approve DRAFT/LIVE.
- **Q.62.E (7 commits)** — Cleanup pós-fecho: KPIFactory adoption no
  Copilot source-of-truth, PlaneamentoPage polling, orval generated/
  versionados, RLS prod role provisioning script, Q.55 experiment
  files arrumados.

---

## Tier 1 — Bloqueios resolvidos

- **`alembic upgrade head` corre limpo numa DB fresca** até HEAD `057_q62_d_commit_status`. Antes a chain estava partida em ≥5 sítios (governance.approval órfã, factory_meta órfãs, sa.Enum vs postgresql.ENUM, ALTER SYSTEM em tx, tenant_configurations typo).
- **`init_db()` em produção deixa de fazer `create_all`** — apenas verifica revision via Alembic.
- **87 RLS policies** criadas em todas as tabelas com `tenant_id` (script `deploy/postgres/q62_rls_prod_role.sql` para activar em prod).
- **CPO async** — endpoint `/v1/plan/cpo/schedule` deixa de bloquear 30s; `POST /async` enfileira Arq + `GET /job/{id}` polling + `PUT /approve` promove DRAFT→LIVE.
- **KPIFactory consolidado** — `team_defect_rate`, `product_defect_rate`, `oee`, `otd_actual_pct`, `otd_risk` com nomes inequívocos; Copilot `calculate_kpis` agora retorna FPY/rework_rate reais via `product_defect_rate` (antes era `null + reason="QUALITY_DEFECTS_TABLE_UNAVAILABLE"`).

---

## Tier 2 — Mudanças destacadas

### Backend

- **`src/profit/kpi_factory.py`** — 5 métodos consolidados; ambíguo `defect_rate()/oee()/otd()` REMOVIDOS.
- **`src/plan/cpo/scheduler_run.py` (NOVO)** — body extraído de `schedule_cpo` para uso por endpoint sync + Arq worker.
- **`src/plan/cpo/worker.py` (NOVO)** — Arq `WorkerSettings` + `cpo_schedule_job`.
- **`src/shared/auth/tenant_context.py` (NOVO)** — ContextVar + middleware para SET LOCAL app.tenant_id.
- **`src/shared/database.py`** — `@event.listens_for("begin")` injecta `SET LOCAL app.tenant_id` em cada tx.
- **`src/governance/audit_service.py` (Q.61.18)** — pipeline único de audit log.
- **`src/profit/api/kpis.py`** — `calculate_kpis` wirado a `KPIFactory.product_defect_rate`.
- **`src/legacy/` APAGADO** — 7 endpoints migrados para plan/workforce/quality (Q.61.32a-d).

### Alembic migrations

- **028a_q62_governance_orphan** — 5 tabelas governance que viviam só via `create_all`.
- **028b_q62_factory_meta** — 4 tabelas factory_meta (ingestion_run + 3).
- **055a_q62_a3_orphans** — 13 tabelas adicionais (hr.legacy_allocations, plan.production_orders, factory_curated.*, factory_raw.excel_row).
- **056_q62_b_rls_enable** — `ENABLE ROW LEVEL SECURITY` + `CREATE POLICY tenant_isolation` em 87 tabelas.
- **057_q62_d_commit_status** — `plan_schedule_commits.status` DRAFT|LIVE.
- **Bugfixes:** 015_transport_batch (CREATE SCHEMA plan), 018b_create_production_schedules (postgresql.ENUM), 027_decision_rejection_category (rebased to 028a), 028_curated_allocation (rebased to 028b), 036_s3_schema_drift_fixes (singular tenant_configuration), 040_slow_query_logging (autocommit_block).

### Frontend

- **Decomp Q.60** — DirecaoPage, EmployeesPage, QualidadePage, SettingsPage, ExpedicaoPage, EquipaPage, ConfiguracaoPage, MateriaisPage, aprendi/tabs, causal/CausalPanels todos quebrados em sub-componentes ≤500 linhas.
- **PlaneamentoPage.tsx** — adopt `runScheduleAsync` + polling + Aprovar (LIVE) UI.
- **`frontend/src/lib/api/generated/`** — 689 ficheiros orval `gen:api` versionados (single source of truth).

### Tests

- **+150 testes novos** distribuídos por Q.62.A.* (chain), Q.62.B.3 (RLS wiring), Q.62.C.* (KPIFactory), Q.62.D.* (Arq + endpoints), Q.62.E.4 (KPIFactory adoption).
- Canary final: **1500+/0** verde.

### Docs

- **CLAUDE.md** — trim 176→94 linhas (Q.61.40).
- **`agent_docs/`** — sprint_history actualizado, mutmut_target.md, q55_diretor_report.md, pr_descriptions/.

---

## Tier 3 — Tests / Gates (pré-merge)

| Gate | Estado |
|---|---|
| `pwsh scripts/verify.ps1` (gate completo) | ✅ verde |
| `pytest tests/{shared,governance,plan,workforce,quality,profit,copilot}` | ✅ 1500+ |
| `python scripts/lint_drift_gate.py` | ✅ BLE001=366 (baseline) |
| `ruff check src/` | ✅ All checks passed |
| `frontend && npx tsc -b --noEmit` | ✅ exit 0 |
| `frontend && npm test` | ✅ verde |
| Clone fresco DB → `alembic upgrade head` | ✅ chega a 057 |

---

## Tier 4 — Riscos conhecidos

1. **RLS dev superuser bypass** — Postgres dev user é superuser; policies estão criadas mas inertes. Aplicar `deploy/postgres/q62_rls_prod_role.sql` em prod uma vez.

2. **alter_column drift ~400 sites** — `alembic check` mostra que server_default e index renames divergem entre create_all histórico e `Base.metadata` actual. Cosmético (mesmo data); fica para Q.63 cleanup ou aceitar perpetuamente.

3. **`PlaneamentoPage` precisa de Arq worker up** — sem `arq src.plan.cpo.worker.WorkerSettings` a correr, o `POST /schedule/async` enfileira mas ninguém processa. Botão Replanear fica eternamente "A processar...". Mitigação: systemd unit `nelinho-arq.service` em prod; dev arranca manualmente.

4. **3 branches pendentes** — `feat/q44z-erp-integration` (22 commits ERP write path), `feat/q37-nelo-oportunidades` (25 commits 12 features NELO), `feat/nelo-redesign` (93 commits Q.53+Q.54). Após este merge, requerem rebase + resolução de conflitos (Q.60 decomp partiu muito ficheiro).

---

## Test plan (manual smoke)

- [ ] Clone fresco → `alembic upgrade head` → app arranca sem `init_db_create_all()`.
- [ ] Login dev → POST `/v1/plan/cpo/schedule/async` → 202 + job_id (com Arq worker up).
- [ ] GET `/v1/plan/cpo/schedule/job/{id}` → state passa por `in_progress` → `complete`.
- [ ] PUT `/.../approve` → ScheduleCommit.status DRAFT → LIVE.
- [ ] Copilot pergunta "qual o FPY?" → resposta com número real (não "indisponível").
- [ ] PlaneamentoPage botão Replanear → progress badge + Aprovar.
- [ ] Frontend: `npx tsc -b --noEmit` verde.

---

## Commits inclusos (sample, ver `git log --oneline main..HEAD`)

Top 30 commits (mais recentes):

```
4d6b213 Q.62.E.7+E.8+E.9+E.10: tooling sweep — orval, RLS prod, mutmut docs
e1ea605 Q.62.E.6: OEE 4o sitio audit
b7dd5d9 Q.62.E.5: PlaneamentoPage adopta polling async + Aprovar (LIVE)
667cbe2 Q.62.E.4: calculate_kpis usa KPIFactory.product_defect_rate
bf15db7 Q.62.E.2: commitar untracked files
0473b1a Q.62.E.1: noqa BLE001 em Q.62.D + Q.61.32 legacy copies
69ae978 Q.62 close: fecho oficial da campanha + tag q62-closeout
0b8c1ba Q.62.D.2+D.3+D.4: CPO async via Arq, polling, approve DRAFT->LIVE
4f57fb0 Q.62.D.1: arq worker scaffolding + systemd unit
730a3cb Q.62.C: KPIFactory consolidado com nomes inequivocos
38f33a0 Q.62.B: Postgres RLS + event listener + middleware + tests
e68eac1 Q.62.A.3: criar 13 tabelas orfaos
17ac437 Q.62.A.1+A.2: alembic chain upgrade head limpo ate 055
c525173 Q.61.43: fecho oficial da campanha Q.61
... (149 mais)
```

---

## Como rever em chunks

PR de 179 commits é difícil de rever ponto a ponto. Sugestão de ordem:

1. **Q.62.A** (chain repair) — `git log Q.62.A.1..Q.62.A.3`
2. **Q.62.B** (RLS) — 1 commit big (`38f33a0`)
3. **Q.62.C** (KPIFactory) — 1 commit big (`730a3cb`)
4. **Q.62.D** (CPO async) — 2 commits (`4f57fb0`, `0b8c1ba`)
5. **Q.62.E** (cleanup) — 7 commits
6. **Q.61** (28 entregas + 5 no-fix + 7 decisões) — `agent_docs/sprint_history.md` resume tudo
7. **Q.60** (decomp) — mecânico, baixo risco

## Sugestão de merge

**`gh pr merge --merge`** (não squash) para preservar o trail Q.X.Y;
útil para `git log -- <file>` mostrar contexto histórico.
